# AI traceability

Every AI-assisted change is recorded here: what was generated, what the engineer
changed, what was rejected and why, which gates ran, and who signed off.
Maintained by `scripts/ai_log.py`.

| Date | Issue | Commit | Summary | Generated | Edited | Rejected (why) | Gates | Sign-off |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-22 | #1 | 01cffba | Requirements, assumptions, and v1 scope; engineering harness | assumptions.md draft: ambiguity table, API surface, non-goals, environment assumptions | aligned API surface with the contracts already promised in stories #9-#18; added 404 parity for expired/deleted links; added hashed-IP and hashed-API-key assumptions; added base-URL-from-config note | 301 redirects (breaks analytics and takedown); fetching destinations for validation (SSRF); custom aliases in v1 (reserved for the brownfield story); geo/device analytics (not requested, adds PII) | n/a - documentation only, no build yet | pending human review |
| 2026-08-22 | #7 | 47de0e9 | Component architecture, request flows, and relational data model | architecture.md draft: component diagram, create and redirect flows, four-table schema, Redis key table, scale-up path | made expiry a re-check on every cache hit plus a capped TTL rather than an invalidation event; made a failed cache invalidation fail the delete with 500; bounded the click queue with an explicit drop policy; added the DNS-resolution limitation of the URL policy as an accepted risk; added daily salt rotation to the IP hash; reconciled the derived daily rollup with assumptions.md | write-through caching (contradicts Postgres-as-source-of-truth); SELECT-before-INSERT collision check (TOCTOU race); caching API-key lookups (revocation invalidation risk on the one path where slow is fine); a fifth click_daily rollup table (issue fixes the schema at four tables); warming the cache on create (fills Redis with entries nobody reads) | n/a - documentation only, build lands in #8 | pending human review |
| 2026-08-22 | #8 | 3a8e59a | Service skeleton, quality gates, migration runner, and local infrastructure | Fastify app and health route, tsconfig/ESLint/Vitest config, env schema, migration runner and initial schema, docker-compose, README | replaced Fastify's default request serializer so remoteAddress is never logged; fixed logger typing that had silently selected the http2 Fastify overload; added parseEnvOrExit so a bad config prints one line instead of a stack trace; added describeError after finding 'Migration failed:' printed blank for an unreachable database; added a checksum guard rejecting edits to applied migrations; added an advisory lock so concurrent boots cannot double-apply; made the .env.example drift test assert against the schema | dotenv (Node 22 has process.loadEnvFile); pino as a direct dependency (Fastify bundles it); ioredis (no story uses Redis yet, so it would be a dependency with no caller); an ORM or query builder (raw SQL keeps the schema honest against architecture.md); a migration library (the runner is ~90 lines with explicit ledger semantics); dropping the public schema in tests (DATABASE_URL may point at a working database) | typecheck,lint,test,audit | pending human review |
| 2026-08-23 | #9 | 89c8432 | Authenticated URL creation with hashed keys and destination policy | create route, service, repos, URL policy, OpenAPI, integration tests | used savepoints around code inserts after unique violations aborted the transaction; put generateCode on appConfig so Fastify would not bind Vitest mocks; aligned JSON field originalUrl with the issue while keeping destination_url as the column; stripped IPv6 brackets Node leaves on hostname | DNS lookup of destinations (SSRF); SELECT-before-INSERT uniqueness (TOCTOU); caching API-key hashes (revocation lag); reflecting the submitted URL in 400 bodies | typecheck,lint,test,audit | pending human review |
| 2026-08-23 | #10 | a733618 | Public 302 redirect with cache-aside, expiry guards, and no-store | redirect service, memory and Redis caches, GET /:code and GET /api/v1/urls/:code, OpenAPI, tests | re-check expiresAt on cache hits instead of relying on TTL alone; fail-open Redis so an outage cannot change 302 vs 404; reserved-name set for 7-char paths like openapi; expiry integration test uses a frozen clock because the schema forbids expires_at before created_at | 301 (browser-cached, breaks takedown); DNS of destinations (SSRF); blocking the redirect on analytics; treating Redis as source of truth | typecheck,lint,test,audit | pending human review |
| 2026-08-23 | #11 | f0fe51b | Click capture on redirect: hashed IPs, fail-open writes, Redis counters | ClickCapture, IP hash, click_events insert, Redis counters, redirect wiring, tests | record() never awaits I/O so inject/302 cannot wait on Postgres; daily salt in the IP hash so visitors cannot be correlated across days; queue drops oldest at 10k instead of growing without bound | awaiting the insert before 302 (violates the hot path); storing raw IP even in logs; retrying failed inserts (that would be at-least-once, not the accepted v1 trade-off) | typecheck,lint,test,audit | pending human review |
| 2026-08-23 | #12 | 1602f09 | analytics stats endpoint | stats service, windowed SQL aggregate, route, tests, OpenAPI | injected repo functions instead of the pool, deduplicated the days validation error, documented the sparse series and captured the real query plan | unbounded all-time window; in-memory aggregation over click rows; reading totals from Redis counters, which are allowed to drift | typecheck,lint,test | engineer |
